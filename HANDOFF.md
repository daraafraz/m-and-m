# Music & Meaning — Brand Proposal Handoff

See repository root for `music_meaning_proposal_v2.html` — single-file interactive proposal.

**Status:** In Context section has logo + palette wiring (`selectLogo` updates `.ctx-wordmark`; `selectPalette` sets `data-palette` on `body` and each `.ctx-cell`). Add real photos when available — uncomment `<img>` lines in section 05.

**Next steps:** Drop images into each `.ctx-cell`, tune opacity/object-position; replace placeholder concert copy when confirmed.
